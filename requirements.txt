Django==2.0
argparse>=1.2.1
ipaddress>=1.0.22

# Version 3.0 supports Django 1.11 through 2.1
django-registration==3.0

# Version 2.7.0 supports Django 2.0
django-tinymce==2.7.0

# I think the docs say that the version matches your Django version
social-auth-app-django==2.0

# This is the original version. Maybe we need to go higher, not sure.
social-auth-core>=3.0.0,<4.0

# Requests officially supports Python 3.8+
requests>=2.27.1,<3.0

# All of these versions should be okay
lxml>=2.3.2
PyYAML>=5.4
django-sortedm2m>=2.0.0
django-widget-tweaks
semantic_version
